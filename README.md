# SEEQST Shadows

## Setup

```console
python -m venv .venv -r requirements.txt
source .venv/bin/activate 
pip install --upgrade pip
pip install -r requirements.txt 
```

The file `shadows.py` contains the abstract class for classical shadows which the current SEEQST class inherits from. A simple example experiment can be found in `experiments.py`.
